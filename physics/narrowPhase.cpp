#include "narrowPhase.h"

NarrowPhase::NarrowPhase() {
  // to be implemented
}

NarrowPhase::~NarrowPhase() {
  // to be implemented
}

Array<Manifoldf*>* NarrowPhase::collisionChecksf(Array<Manifoldf*>* collisions) {
  // to be extended
  return 0x0;
}

Array<Manifoldd*>* NarrowPhase::collisionChecksd(Array<Manifoldd*>* collisions) {
  // to be extended
  return 0x0;
}

Array<Manifoldf*>* NarrowPhase::collisionResolutionf(Array<Manifoldf*>* collisions) {
  // to be extended
  return 0x0;
}

Array<Manifoldd*>* NarrowPhase::collisionResolutiond(Array<Manifoldd*>* collisions) {
  // to be extended
  return 0x0;
}
